/* Estrutura de repeticao: Do While
        semelhante ao while, porem a estrutura muda um pouco;
        esse metodo quase nao é utilizado;
                exemplo:
                    let y = 0;
                        do  {
                            console.log(y);
                                y = y + 1;
                        } while(y < 5);
        While = Enquanto
*/
    let x = 100;
        do {
            console.log(x/2);
                x = x - 5;
        } while (x >= 0);
